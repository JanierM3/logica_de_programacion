#include <iostream>

using namespace std;

int main(){
    /*
    // Calcular el area de un circulo 
    float radio;
    float area;

    cout << "Ingrese el radio del circulo";
    cin >> radio;

    area = 3.141559 * radio * radio;
    cout << "El area del circulo es: " << area;
    */

    /*
    // Ordenar una lista de numero de menor a mayor
    int lista[5];
    int i, j, auxiliar;

    for(i = 0; i < 5; i++){
        cout << "ingrese el numero " << i + 1 << ": ";
        cin >> lista[i];
    }

    for (i = 0; i < 4; i++)
    {
        for (j = 0; j < 4 - i; j++)
        {
            if (lista[i] > lista[j + 1])
            {
                auxiliar = lista[j];
                lista[j] = lista[j + 1];
                lista[j + 1] = auxiliar;     
            } 
        } 
    }

    cout << "La lista ordenada es: ";
    */
}
